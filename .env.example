# Copy to .env.local and adjust for your environment.
# In Next.js, only variables prefixed NEXT_PUBLIC_ are exposed to the browser.

# Base URL of the blog REST API (json-server in development; the frontend still
# uses this until it is rewired to the new /api routes).
NEXT_PUBLIC_API_URL=http://localhost:8000/

# ---- New backend (Prisma + Auth.js) — see docs/BACKEND.md ----
# Postgres connection string (e.g. from Neon). Required by the Prisma backend.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
# Secret used to sign Auth.js (NextAuth) JWT sessions.
# Generate one with:  openssl rand -base64 32
AUTH_SECRET=

# Cloudinary unsigned upload settings (image uploads go directly from the browser).
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=dvnwx89ao
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=cg4zfcut

# Optional: enables the AI "Cook From Your Pantry" recipe generator.
# WARNING: this key is exposed in the browser bundle (demo only). For production,
# proxy Anthropic calls through a server route so the key is never exposed.
# Leave unset to disable AI generation (the pantry matcher still works).
NEXT_PUBLIC_ANTHROPIC_API_KEY=
# Optional model override (defaults to claude-opus-4-8; e.g. claude-haiku-4-5 for cheaper/faster).
NEXT_PUBLIC_ANTHROPIC_MODEL=claude-opus-4-8
